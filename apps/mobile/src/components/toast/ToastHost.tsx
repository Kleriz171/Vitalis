/**
 * Deprecated — sonner's <Toaster/> in App.tsx renders notifications now.
 * This stays as a no-op shim so existing imports keep building. Remove
 * the JSX usage in App.tsx and you can delete this file.
 */
export const ToastHost = () => null;
